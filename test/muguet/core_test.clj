(ns muguet.core-test
  (:require [clojure.test :refer [deftest is testing]])
  (:require [muguet.core :as sut]))

(deftest hatch-ok-test
  (testing "A new aggregate-root can be hatched"
    (let [schema ])))
